class Solution {
    public int[] buildArray(int[] nums) {
        int n = nums.length;
        int num[] = new int[n];
        for(int i=0;i<n;i++){
            num[i] = nums[nums[i]];
        }
        return num;
    }
}
